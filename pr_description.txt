🎯 **What:** The `frontend/src/utils/fallbackData.js` utility lacked test coverage for its exported mock data. This PR adds unit tests for `mockDestinations` to ensure its structural integrity.
📊 **Coverage:** The test now verifies that `mockDestinations` is a non-empty array, checks that each destination object adheres strictly to the expected schema (correct data types for id, city, country, price, rating, category, image, description, amenities, and reviews), validates that all nested `amenities` are strings, and ensures every destination has a unique `id`.
✨ **Result:** Test coverage for `fallbackData.js` goes from 0% to fully covered, ensuring the application handles fallback data safely and any future changes to this data structure will not silently break the app when the backend is unavailable.
